# WebSocket 服务器配置
WS_PORT=8080
WS_AUTH_TOKEN=your-secret-token-here

# HTTP 服务器配置（用于展示页面）
HTTP_PORT=10089

# Redis 配置（可选，用于缓存和消息队列）
REDIS_ENABLED=1
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# PostgreSQL 配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hgwss
DB_USER=hgwss
DB_PASSWORD=your-strong-password

# 滚球账号配置
LIVE_CROWN_USERNAME=live_account
LIVE_CROWN_PASSWORD=live_password
LIVE_ACCOUNT_POOL=hg339352/2OLqr44s,hg387962/xGeZj3fx,hg841992/YW07Z7lu

# 今日账号配置
TODAY_CROWN_USERNAME=today_account
TODAY_CROWN_PASSWORD=today_password
TODAY_ACCOUNT_POOL=

# 早盘账号配置
EARLY_CROWN_USERNAME=early_account
EARLY_CROWN_PASSWORD=early_password
EARLY_ACCOUNT_POOL=

# 抓取间隔（秒）
LIVE_FETCH_INTERVAL=5          # 滚球：每5秒抓取一次
TODAY_FETCH_INTERVAL=10        # 今日：每10秒抓取一次
EARLY_FETCH_INTERVAL=3600      # 早盘：每1小时抓取一次

# 账号轮换间隔（分钟）
LIVE_ROTATION_MINUTES=30       # 滚球：每30分钟换一次账号
TODAY_ROTATION_MINUTES=60      # 今日：每60分钟换一次账号
EARLY_ROTATION_MINUTES=60      # 早盘：每60分钟换一次账号

# 日志级别
LOG_LEVEL=info

# 皇冠 API 配置
CROWN_API_BASE_URL=https://api.example.com
CROWN_SITE_URL=https://www.example.com

# API 超时配置（毫秒）
API_TIMEOUT_MS=60000                    # API 请求超时时间，默认60秒

# 多盘口抓取配置
ENABLE_MORE_MARKETS=1                    # 是否启用更多盘口抓取（1=启用，0=禁用）
MORE_MARKETS_START_DELAY_SECONDS=45     # 登录后延迟多少秒开始抓取更多盘口
MORE_MARKETS_INTERVAL_MS=25000          # 抓取更多盘口的间隔（毫秒）
MORE_MARKETS_MAX_CONCURRENCY=1          # 最大并发数
MORE_MARKETS_LIMIT=1                    # 每次抓取的比赛数量限制
